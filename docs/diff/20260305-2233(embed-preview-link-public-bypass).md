# Summary
- Added admin preview bypass in /api/widget/config when preview=1 to allow template preview even if is_public=false.
- Added template preview link (chat_policy override via ovr) to conversation preview tab.

# Files
- src/app/api/widget/config/route.ts
- src/app/app/conversation/page.tsx
