# 질문
현재 답변을 reply에 출력해주세요

# 답변
요청대로 **두 테이블을 하나로 합치는 방식**으로 변경했습니다.
`user_profiles` + `org_members` 대신 **`user_access` 단일 테이블**로 통합하고, 값 입력/조회 코드도 전부 반영했습니다.

변경 내용
- 스키마 통합: `docs/user_schema_patch.sql` → `user_access` 단일 테이블
- 사용자 프로필 API가 `user_access`에서 읽도록 변경
- 온보딩 완료 시 `user_access`에 권한/플랜/역할 기록

변경 파일
- `docs/user_schema_patch.sql` (단일 테이블로 교체)
- `src/app/api/user-profile/route.ts` (user_access 조회)
- `src/app/onboarding/page.tsx` (온보딩 시 user_access insert)
- `docs/단계별_구현_체크리스트_및_검증.md` (항목 문구 수정)

다음 작업(필수)
1) Supabase SQL Editor에서 `docs/user_schema_patch.sql` 실행
2) 기존 `user_profiles`, `org_members`가 이미 생성되어 있다면,
   - 파일 하단의 `drop table if exists ...` 주석을 풀고 실행

이후부터는 모든 권한/플랜/역할이 `user_access` 한 테이블에서 관리됩니다.

원하시면 다음으로 “실제 통화 처리 로직(턴/이벤트 로그 흐름 강화)”의 프론트 트리거를 이어서 진행하겠습니다.
