# 질문
실행해주세요

#답변
요청하신 Cafe24 Admin API 호출을 실행했습니다. 두 호출 모두 401 오류로 실패했고, 메시지는 `Invalid mall_id (invalid_token)`입니다. 제공된 토큰의 mall_id는 `mejai`인데, 요청은 `sungjy2020.cafe24api.com`로 보내서 mall_id 불일치로 보입니다. 토큰이 `sungjy2020`에 대한 것인지, 혹은 `mejai`로 호출해야 하는지 확인이 필요합니다.

현재 실행 결과(터미널 출력 포함):
```
PS C:\dev\1227\mejai> $token = "SCG6997JDZLoo33vnMjtGA"; Invoke-RestMethod -Method Get -Uri "https://sungjy2020.cafe24api.com/api/v2/admin/customers?shop_no=1&member_id=mejai" -Headers @{ Authorization = "Bearer $token" } | ConvertTo-Json -Depth 10
Invoke-RestMethod:
Line |
   2 |  … 33vnMjtGA"; Invoke-RestMethod -Method Get -Uri "https://sungjy2020.ca …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     |
{
  "error": {
    "code": 401,
    "message": "Invalid mall_id (invalid_token)"
  }
}

PS C:\dev\1227\mejai> $token = "SCG6997JDZLoo33vnMjtGA"; Invoke-RestMethod -Method Get -Uri "https://sungjy2020.cafe24api.com/api/v2/admin/orders/20260127-0000014?shop_no=1" -Headers @{ Authorization = "Bearer $token" } | ConvertTo-Json -Depth 10
Invoke-RestMethod:
Line |
   2 |  … 33vnMjtGA"; Invoke-RestMethod -Method Get -Uri "https://sungjy2020.ca …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     |
{
  "error": {
    "code": 401,
    "message": "Invalid mall_id (invalid_token)"
  }
}
```

다음 중 무엇이 맞나요?
1) mall_id는 `sungjy2020`이 맞고, 해당 mall용 액세스 토큰을 새로 제공
2) mall_id는 `mejai`가 맞고, `https://mejai.cafe24api.com`로 다시 호출