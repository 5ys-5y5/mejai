# 질문
1. railway 배포 문제가 해결되었습니다.
https://websocket-server-production-e11f.up.railway.app/
ws-server ok
.env 파일에 저장함
NEXT_PUBLIC_CALL_WS_URL = "wss://websocket-server-production-e11f.up.railway.app/"

2. 현재 결정된 사항을 bot_spec.md에 업데이트해주세요.

3. 기본 임베딩 모델 text-embedding-3-small로 진행해주세요.
(임베딩 전용 호출은 OPENAI로만 사용하도록 하겠습니다. 이 역시 bot_spec.md에 기록해주세요)

4. 다음 단계로 KB 임베딩 파이프라인 + LLM/MCP/KB 연동 구현을 진행해주세요.

#답변
요청대로 반영했습니다.

변경 사항
- `docs/bot_spec.md`: 결정 사항(WS 분리 운영, 주문 조치 범위, 임베딩 모델/전용 OpenAI, KB/MCP 제한 규칙) 업데이트
- `quick_start.md`: WS 서버 실행 및 `APP_BASE_URL` 안내 추가
- KB 임베딩 파이프라인: KB 생성/수정 시 OpenAI `text-embedding-3-small`로 즉시 임베딩 생성
- Playground LLM/MCP/KB 연동: `/api/playground/chat` 추가 및 WS 서버에서 호출
- Playground UI: WS 연결 + 실제 응답 수신으로 변경
- MCP 호출: 에이전트에 연결된 MCP 도구만 실행하도록 제한

추가 안내
- 기존 KB는 embedding이 null이므로, `/api/kb/reindex`로 재임베딩 가능(최대 200개/요청)
- WS 서버 환경변수에 `APP_BASE_URL=https://mejai.help` 설정 필요
